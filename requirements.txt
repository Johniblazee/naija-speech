# Core ML
torch>=2.1
transformers>=4.46,<5  # >=4.46 for Trainer(processing_class=...); <5 because v5
                       # requires a newer torch (DTensor) than some pod images ship —
                       # our code runs identically on 4.46+ either way
datasets>=2.20,<4.0   # AfriSpeech-200 uses a loading script (removed in datasets 4.x)
accelerate>=0.33
peft>=0.12

# Audio
librosa>=0.10
soundfile>=0.12

# Metrics
jiwer>=3.0
evaluate>=0.4

# Experiment tracking
wandb>=0.17

# EDA + utilities
pyyaml>=6.0
pandas>=2.0
matplotlib>=3.7
tabulate>=0.9        # pandas .to_markdown() for the EDA report
duckdb>=1.0          # remote columnar corpus reads (07/08 — no 70GB download)
tqdm>=4.66
huggingface_hub>=0.34  # recent enough for the `hf` CLI used in the pod walkthroughs

# Optional (GPU/Linux only) — install on Colab/RunPod:
# unsloth                 # faster/lower-VRAM Whisper fine-tuning (primary STT backend)
# bitsandbytes>=0.43      # 8-bit loading of whisper-large-v3 (QLoRA-style)

# TTS scoring (10_tts_score.py) runs in THIS venv; optional naturalness metric:
# pip install "git+https://github.com/sarulab-speech/UTMOSv2.git"

# TTS synthesis (09_tts_synthesize.py) — per-model venvs ONLY, NEVER into this
# venv (dependency trees conflict; full session: docs/runpod-guide.md §3.6):
#   venv-xtts:  coqui-tts
#   venv-qwen:  qwen-tts
#   venv-yarn:  outetts==0.2.3 uroman gdown
#               + clone github.com/saheedniyi02/yarngpt into third_party/
#               + WavTokenizer config (HF novateur/...) + ckpt (gdown) in third_party/
