# Copy to .env and fill in. Never commit .env.

# Weights & Biases (experiment tracking). Get a key at https://wandb.ai/authorize
WANDB_API_KEY=
WANDB_ENTITY=
WANDB_PROJECT=naija-speech

# Hugging Face token (needed to download some datasets/models)
# https://huggingface.co/settings/tokens
HF_TOKEN=
